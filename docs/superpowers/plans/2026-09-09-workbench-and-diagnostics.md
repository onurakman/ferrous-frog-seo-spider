# Workbench And Diagnostics Implementation Plan

Continue the approved roadmap in successive verified increments without a new approval checkpoint. Preserve existing changes and the compact crawl library. Latest steering: stop at the current verified checkpoint after manual PageSpeed integration; do not start another roadmap item.

## Current work

- [x] User steering: center first launch, remember window geometry/maximize state, retain splash sequencing and recover from unavailable monitor positions. Two native policy/geometry tests pass; real Windows/macOS multi-monitor testing requires those desktop environments.
- [x] Grid: individual column visibility, keyboard-accessible ordering, named saved layouts, safe persistence and an automatic relevant-columns default.
- [x] Crawl scope: optionally check links outside the start folder once without recursively crawling those pages.
- [x] Requests: validated non-secret custom headers, restricted to the configured origin, with settings persistence and clear errors.
- [x] Canonicals: typed target, chain and loop diagnostics with Memory/SQLite query parity, counts and audit views.
- [x] Exports: selected URL records and queued URLs through bounded native queries; clipboard access with error feedback.
- [x] Integrate settings, audit and export controls, add browser regression checks, and run the required CI checks. `make ci` passed after integration: 258 default Rust tests, two browser-discovery tests, five real-Chrome fixtures, browser UI smoke, Clippy, format, release/version checks and production build (largest JS chunk 362.35 kB).

## Following increments

- [x] Stream filtered XLSX through the existing atomic worker writer, preserving filters/sort/List order and enforcing Excel's row limit. Retain a bounded bytes-returning compatibility command.
- [x] Content region include/exclude selectors and bounded pasted-HTML preview using the same parser as crawls. Core/parser/native tests and the browser draft/preview/persistence regression pass.
- [x] Image-alt CSV and bounded shared image queries; direct graph JSON file serialization. Native escaping/paging/idle/atomic fixtures and browser menu tests pass.
- [x] Record-only SQLite evidence revisions to avoid summary/graph rebuilds after frontier writes, with measured before/after workload.
- [x] Typed advanced grid filter groups, native validation and an Apply/Cancel builder UI. Storage/export tests and the real browser grid/tree/export/cancellation checks pass.
- [x] Shared Retry-After handling within existing retry budgets and cancellation policy. Core tests plus a real Chrome origin-cooldown fixture pass; browser notices clear at crawl completion.
- [x] Exact-response-hash duplicate audit, distinct-final-URL eligibility, bounded storage queries, typed issues and grid/workbook evidence. Storage/analysis/export tests and browser audit/count checks pass; rendered/selected-content semantics remain distinct.
- [x] Stream graph node/edge CSV through the existing atomic worker writer, retaining current graph bounds. Native Memory/SQLite, Unicode, empty-report, limit and atomic publication checks pass.
- [x] Add per-type alternate-link discovery controls with safe default/profile migration. Core/parser, real Chrome fixtures and frontend persistence/unavailable-mode checks pass.
- [x] Fix live URL-tree refresh starvation when storage queries exceed the polling interval; the delayed-response browser regression passes.
- [x] Latest UI steering: improve the splash and minimum display duration, move card deletion upper-right, soften the dark palette and repair Settings navigation. Theme/responsive/card/splash smoke checks pass, including the reviewed nested-preview-error footer regression.
- [x] Follow-up UI steering: show saved crawls in a bottom disclosure panel, initially closed with a persisted user preference. Center the launcher while closed; preserve search, comparison, deletion and visible history errors. Browser checks cover keyboard focus, reload persistence, reduced motion and a 390×400 error state. The combined `make ci` run passes: 278 default Rust tests, two browser-discovery tests, six real-Chrome fixtures, browser smoke, Clippy, format/release/version checks and production build (largest JS chunk 365.97 kB).
- [x] Redesign the crawl graph around a large section-cluster canvas, searchable URL navigation, node relationships and camera controls, preserving bounded snapshots and accurate broken/redirect filters. WebGL/SVG, both themes, narrow selection, stable live/theme camera geometry and keyboard navigation pass real-browser checks. Remove the unused layout packages.
- [x] Extend motion across disclosure groups, side panels and control interactions, including retained dialog/menu exits, stable closing content, focus restoration and reduced-motion behavior. Keep live grid rows stable. Real-browser regressions cover rapid reopening, nested dialogs, menu handoffs, immediate Escape/Enter and deletion focus.
- [x] Remove measured Memory ingestion bottlenecks in URL alias lookup and progress summaries, preserve alias/List semantics, and rerun the local crawler benchmark. All 90 storage and 93 core tests plus scoped Clippy pass; uninterrupted/stop-resume runs fall from 27.103/26.496 s to 6.572/6.603 s with identical record, edge and request counts.
- [x] Add a bounded pasted-HTML tester for one custom extraction rule using the existing extraction code and preview worker. All 12 extractor and 51 native tests plus scoped Clippy pass. A browser review verifies that the worker guard survives closing/reopening Settings and stale results are discarded. Sample HTML and preview results are not saved.
- [x] Page sitemap-validation reports directly in SQLite without loading full crawl records, preserving issue wording, search/sort and List occurrence order. All 95 storage tests, scoped Clippy, the sitemap CSV fixture and native paging/failure regression pass. Five 200-row queries over 20,000 records fall from about 16 seconds to 0.3 seconds; candidate metadata is still scanned.
- [x] Native image/link/anchor/sitemap queries use guarded blocking workers and fallible dispatch shared with exports. All 49 native tests and Clippy pass; queries remain uncancellable. SQLite sitemap paging is now implemented in the following increment above.
- [x] Add direction-specific pagination target-error audits using observed response evidence, existing rel-next/previous fields and cached aliases. Preserve unknown/robots targets, successful redirects, List occurrences, bounded SQLite queries and typed issue output.
- [x] Add an AMP target-error audit using the same observed-target evidence and captured `amphtml` field. Preserve unknown/robots targets and redirect/List semantics; AMP markup validation and reciprocity remain separate work.
- [x] Capture nullable title/description tag counts and add multiple-tag warning views, with parser/core/storage/analysis parity, sortable grid evidence and CSV/XLSX output. Preserve retained text and keep historical unknown counts distinct from measured zero. Four-crate tests/Clippy, all 20 export tests, the complete UI smoke and an independent compatibility review pass.
- [x] Preserve List file-import occurrences and order through the Settings draft, matching pasted lists and existing engine behavior. The complete browser smoke verifies append order, repeated entries, draft isolation and Apply persistence.
- [x] Bound graph snapshot construction before copying records in Memory/SQLite, preserving existing literal URL/List ordering and placeholders. Share a fallible query between native background reads and atomic exports. Seven focused graph tests, all 113 storage tests, 52 native tests, 20 export tests, browser recovery/camera checks and an independent review pass. In the local 50,000-record workload, graph latency drops from 178 to 14.5 ms in Memory and from 2,160 to 57.3 ms in SQLite.
- [x] Detect next/previous pagination loops separately, including self-targets and paths entering a same-direction cycle. Reuse observed aliases and cached diagnostics; normal next/previous reciprocity must remain valid. All 117 storage and 10 analysis tests, scoped Clippy and the complete UI smoke pass, including source counts, typed issues and progress retention.
- [x] Align retained title/meta/heading values with document-only counts, preventing SVG labels and inert template metadata or robots directives from contaminating page audits. Preserve real document order, repeated robots merging, property fallback and content-region independence. All 22 parser and 96 core tests and scoped Clippy pass.
- [x] Exclude inactive template and foreign-namespace relation links from captured targets and discovery, following the independent review's reproduced false pagination-loop case. All 25 parser tests and Clippy pass; canonical/hreflang counts and sitemap/reference discovery share the same active-element predicate.
- [x] Latest user steering: use editable Chrome desktop request defaults, preserve saved explicit settings and provide Chrome/Ferrous Frog restore actions. The full UI smoke proves draft/reset/cancel/restart behavior; 97 core tests, two rendering discovery checks and seven real-Chrome fixtures verify default/custom headers, origin restrictions and native resource negotiation. Omit unsupported signed exchanges from HTTP Accept; leave encoding and Sec-* headers to the client/browser.
- [x] Finish integrated verification after recording panel transition events before clicks and awaiting splash document/stylesheet/font readiness before layout checks. A controlled stylesheet delay reproduces the previous splash failure without a product layout regression; complete CI passes with both timing races removed.
- [x] Prepare SQLite queue/seen INSERT statements once per checkpoint transaction, preserving complete replacement, List order, deduplication and rollback. All 119 storage tests, scoped Clippy and the core resume compatibility fixture pass. The same public-method workload measures 66–71% less checkpoint persistence time; full frontier cloning/replacement and summary scans remain open.
- [x] Export complete schema-1 crawl archives through an idle background worker and atomic buffered writer. Page SQLite records and all edge/image results, preserve current record order and frontier data, remove the silent one-million-item truncation and avoid the full JSON output buffer. All 58 native tests, scoped Clippy, complete UI smoke and an independent correctness review pass. Retain the existing Memory record snapshot until raw Memory paging is available; full frontier hydration and import/comparison buffers remain.
- [x] Reduce SQLite progress-summary work by combining existing count predicates and avoiding repeated populated duplicate-text normalization. Four new semantic/work fixtures, existing cache regressions, the combined 127 storage tests and scoped Clippy pass. Matched public-method medians over 10,000 records improve 17–22%; counters, event timing, eligibility and revision invalidation are unchanged. The matched local-crawler follow-up is recorded with integrated verification below.
- [x] Add direction-specific pagination reciprocity warnings using observed complete HTML targets and existing redirect/List aliases. Four new storage fixtures, typed Warning coverage, the combined 127 storage/11 analysis tests, scoped Clippy, source review and complete UI smoke pass. Unknown return targets and self-pagination stay unclassified by these warnings; first-captured-relation/multiple-sequence limits are documented.
- [x] Schedule live graph refreshes after the preceding query completes, matching grid/tree polling. The browser regression reproduces starvation with a two-second query, then passes with the correction. Complete UI smoke and independent review pass; camera state, final crawl refreshes and error recovery are retained.
- [x] Recognize the standard's legacy `rel=previous` synonym in previous-URL capture and optional pagination discovery. The new parser regression first reproduced the wrong retained target; all 26 parser and 97 core tests now pass, including case/order, inert/foreign elements, invalid targets, content isolation and existing discovery controls.
- [x] Correct the PageSpeed provider's lab-metric semantics before adding its UI: keep INP unavailable, parse TBT separately, reject absent/failed Lighthouse analyses, preserve genuine zero values and strip request URLs from transport/decode errors. All 11 integrations tests and scoped Clippy pass, including red-first local fixtures and a sentinel-key regression. Keyring/UI/persistence remain the next slice.
- [x] Align SQLite metadata blank checks with Memory for imported/directly stored tabs and Unicode whitespace, reusing existing `ff_trim` in ten shared view predicates. Four red-first fixtures cover missing/short/narrow/same-as-H1 views, summary parity, advanced-filter composition, List records and eligibility. All 131 storage tests and scoped Clippy pass; interior spacing, ASCII case rules and stored text are preserved. Fresh crawl parsing already normalizes these fields. Follow-up summary measurements and their variance limits are recorded in BENCHMARKS.md.
- [x] Add manual PageSpeed measurement for one selected URL, with mobile/desktop choice, OS-keyring credential status/save/clear, bounded cancellable requests and the latest lab snapshot retained in the crawl. Reuse native lifecycle guards and selected-record access; store metrics without keys in Memory, SQLite and archives. All 25 integrations, 136 storage and 67 native tests pass with scoped Clippy. Browser checks cover credential drafts, transport failures, missing/invalid metrics, stale selection, cancellation, both themes and narrow controls. Early cancellation and shutdown admission races have deterministic regressions. Real Google requests and OS credential-store writes were not exercised; bulk measurement and field Core Web Vitals remain separate work.
- [x] Measure and cache SQLite image-record aliases across query/export pages. The focused invalidation regression, all 85 storage tests and Clippy pass; repeated pages improve about 36% in the documented local debug workload.
- [x] Add and run a crawler-level local-site load test with cycles, duplicate URLs, planted errors, robots exclusions and stop/reopen/resume for Memory and SQLite. All four optimized runs finish with 1,030 records/7,110 edges; ordinary core tests and all-feature Clippy pass. Document timings and the debug Memory timeout without making large-site capacity claims.
- [x] Update user-facing documentation and roadmap statuses for the verified work above; continue updates with each following increment.

## Implementation constraints

Use existing dependencies and query/persistence helpers. Keep the dataset in the engine. Validate stored layouts and request inputs. Retain robots, rate limits, cancellation and original URL identity. Reuse the existing browser smoke runner and focused Rust fixtures; no commits or publication are part of this work.

Engine, storage diagnostics and export implementation are delegated by independent file ownership. The main agent owns frontend integration, browser checks, review and documentation.

The integrated graph, motion, extraction-preview, sitemap-query, pagination and AMP checkpoint passes `make ci`: 307 default Rust tests, two browser-discovery tests, six real-Chrome fixtures, graph model checks, browser UI smoke, Clippy, formatting, release/version checks and the production build (largest JavaScript chunk 356.76 kB).

The following metadata-count, List-import and bounded-graph checkpoint also passes `make ci`: 323 default Rust tests, two browser-discovery tests, six real-Chrome fixtures, graph model checks, complete UI smoke, Clippy, formatting, release/version checks and the production build (largest JavaScript chunk 357.48 kB). Updated synthetic screenshots were inspected; native Windows/macOS window-manager testing remains open.

The pagination-loop, active-document extraction and Chrome-request-default checkpoint passes `make ci`: 338 default Rust tests, two browser-discovery checks, seven real-Chrome fixtures, graph checks, complete UI smoke, Clippy, formatting, release/version checks and production build (largest JavaScript chunk 358.80 kB). An independent review found no remaining issues in the header defaults or corrected relation extraction. Both themes and the narrow HTTP-header layout were inspected.

The frontier-checkpoint and complete-archive-export checkpoint passes `make ci`: 346 default Rust tests, two browser-discovery checks, seven real-Chrome fixtures, graph checks, complete UI smoke, Clippy, formatting, release/version checks and production build (largest JavaScript chunk 358.88 kB). Independent review found no remaining correctness issues; the documented full-frontier and Memory snapshot limits remain.

The progress-summary, pagination-reciprocity, legacy-previous, graph-polling and PageSpeed-provider-correction checkpoint passes `make ci`: 362 default Rust tests, two browser-discovery checks, seven real-Chrome fixtures, graph checks, complete UI smoke, Clippy, formatting, release/version checks and production build (largest JavaScript chunk 359.62 kB). Matched local crawler observations retain all record, edge and request counts; timings and their limits are recorded in [BENCHMARKS.md](../../BENCHMARKS.md#fresh-sqlite-progress-summaries).

Stopped at the user's requested checkpoint after manual PageSpeed integration and Unicode metadata parity. Final CI stages pass: 394 default Rust tests, two browser-discovery checks, seven real-Chrome fixtures, graph checks, complete UI smoke, Clippy, formatting, release/version checks and production build (largest JavaScript chunk 367.29 kB). The first run reached rendering checks with stale local storage metadata lacking the new PageSpeed field; cleaning only that package's build outputs and rerunning `make test-rendering` passed without a source change. Browser checks now observe dialog exit events before clicks and await finite layout transitions before measuring responsive controls; product animation durations are unchanged. Both PageSpeed themes at desktop and narrow widths were inspected. No further roadmap work, commit or publication was started.
